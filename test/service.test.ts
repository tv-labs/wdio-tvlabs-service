import { describe, it, expect } from 'vitest';

import TVLabsService from '../src';

describe('TVLabsService', () => {
  it('should be a function', () => {
    expect(TVLabsService).toBeInstanceOf(Function);
  });
});
